/**
 * TODO: Import utility functions from `./utils.js`.
 *
 * TODO: Use `electionData` with each utility function to display the results.
 * You'll need to use `console.info()` to display the results.
 * You can comment/uncomment as you work so that you can focus on one function at a time,
 * but before your final commits, be sure to have all utility functions in use with the
 * necessary `console.info()` calls.
 */

import electionData from "./db.js";

console.info(electionData);
